H. บทที่ 16.4 Commit Schema

create table comments(
comments_id int auto_increment primary key,
comments_text varchar(255),
user_id int  not null,
video_id int not null,
Create_at timestamp default now(),
foreign key(user_id) references User(user_id),
foreign key(video_id) references Video(video_id)
);


insert into comments (comments_text,user_id,video_id)
value('I love the A',1,2),('I lover the B',2,1),('I love the C',3,1) 
;
